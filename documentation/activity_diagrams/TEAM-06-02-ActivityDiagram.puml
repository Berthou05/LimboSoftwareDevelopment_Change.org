@startuml
|User|
start
:Clicks the “Add” button in the Team Members module;
|System|
:Generates a popup to search for an employee and write their role in the team;

if(Did the User click the “Close” button?) is (yes) then
    stop
else(no)
|User|
:Writes an employee name in the search bar;
|System|
if(Did the User click the "Search" button) is(yes) then

else(no)
    stop
    note left
        Go back to Did the User click the "Close" button?
    end note
end if


|System|
:Sends a query with the employee name to database;
|Database|
:Returns matching employees;
|System|
:Shows query results;

if(Did the User select an Employee from the search?) is (no) then
    |System|
    stop
    note left
    Go back to Did the User click the "Close" button?
    end note
    
else(yes)
    

end if
|User|
:Enters its role as a string;
|System|
if(Did the User click the "Save" button) is(yes) then

else(no)
    stop
    note left
        Go back to Did the User click the "Close" button?
    end note
end if
    |System|
    if(Is the role input empty?) is (yes) then
    |System|
    :Button "Save" gets disabled;
    :Shows the message "Some obligatory fields are empty";
    stop
    note right
        Return to User enters its role as string
    end note
    

    else(no)
    |System|
    :Accepts the User input;
    end if

|System|
:Creates a new EmployeeTeam object;
|Database|
:Stores the EmployeeTeam information;
|System|
:Gathers Team information through queries;
|Database|
:Returns queries response;
|System|
:Renders the Team Page with updated information;
:Displays a success message;

stop
@enduml