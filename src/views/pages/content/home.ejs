<div class="grid gap-4 sm:gap-6 lg:grid-cols-[2fr_1fr]">
  <%- include('../partials/HOME/welcomeCard', { employee }) %>

  <section class="space-y-4 sm:space-y-6">
    <%# Report generator section for quick report access from home - full layout with type selector and search. %>
    <%- include('../partials/reportGenerator/reportGenerator', { redirectTo: '/home', defaultReportType, defaultSubjectId, reportSubjects, latestReports, quickReport, layout: 'full' }) %>

    <%- include('../partials/HOME/yourActivity', { latestActivitySections }) %>        
  </section>

  <section class="space-y-6">
    <%# Team Activity - contributions style feed %>
    <%- include('../partials/HOME/teamActivity', { teamActivitySections }) %>
            
    <%# Your Projects with nested highlights and achievements per project %>
    <%- include('../partials/HOME/yourProjects', { projectSections }) %>    
  </section>
</div>
