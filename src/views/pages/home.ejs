        <div class="mx-auto max-w-7xl p-6">
            <div class="mb-8 flex items-center gap-4 rounded-2xl border border-brand-secondary bg-white p-6 shadow-panel">
                <div class="h-14 w-14 overflow-hidden rounded-full border-2 border-brand-primary/20 bg-brand-secondary/30">
                    <img src="<%= account.image %>" alt="Profile" class="h-full w-full object-cover">
                </div>
                <div>
                    <h1 class="font-heading text-2xl font-bold text-brand-text">Welcome, <%= employee.names %></h1>
                    <p class="text-sm text-brand-text/60">Aquí tienes un resumen de lo que sucede hoy.</p>
                </div>
            </div>

            <div class="grid gap-6 lg:grid-cols-[2fr_1fr]">
                
                <div class="space-y-6">
                    
                    <%- include('../partials/HOME/reportGenerator', {
                        layout: 'full', 
                        redirectTo: '/home',
                        defaultReportType: 'EMPLOYEE',
                        reportSubjects: reportSubjects,
                        csrfToken: csrfToken
                    }) %>

                    <div class="rounded-2xl border border-brand-secondary bg-white p-5 shadow-panel">
                        <h2 class="font-heading text-lg font-semibold text-brand-text">Your Latest Activity</h2>
                        </div>
                </div>

                <div class="space-y-6">
                    <div class="rounded-2xl border border-brand-secondary bg-white p-5 shadow-panel">
                        <h2 class="font-heading text-lg font-semibold text-brand-text">Team Activity</h2>
                    </div>

                    <div class="rounded-2xl border border-brand-secondary bg-white p-5 shadow-panel">
                        <h2 class="font-heading text-lg font-semibold text-brand-text">Your Projects</h2>
                    </div>
                </div>

            </div>
        </div>