<%# Template: partials/pageHeader.ejs
   Purpose: Reusable page title + subtitle header.
   Expected locals: title, subtitle.
   Use this partial to keep page headings visually consistent.
%>

<div class="mb-6">
  <h1 class="font-heading text-3xl font-extrabold text-brand-text"><%= title %></h1>
  <p class="mt-1 text-sm text-brand-text/70"><%= subtitle %></p>
</div>

